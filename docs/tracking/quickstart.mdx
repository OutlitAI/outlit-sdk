---
title: "Quick Start"
description: "Install the Outlit CLI, connect your first source, and query customer context in minutes"
---

## What is Outlit?

Outlit is infrastructure for customer context. It connects website activity, product usage, billing, support, and conversation data, then structures it into customer profiles your team and agents can query from the CLI, MCP, or API.

## Step 1: Install the CLI

Fastest path:

```bash
curl -fsSL https://outlit.ai/install.sh | bash
```

<Tip>
  This installs the latest Outlit CLI release from GitHub. Prefer one of the options below if you want manual package management.
</Tip>

<Tabs>
  <Tab title="npm">
    ```bash
    npm install -g @outlit/cli
    ```
  </Tab>
  <Tab title="Homebrew">
    ```bash
    brew install outlitai/tap/outlit
    ```
  </Tab>
  <Tab title="Binary">
    Download the latest release from [GitHub Releases](https://github.com/OutlitAI/outlit-sdk/releases) and add it to your `PATH`.
  </Tab>
</Tabs>

## Step 2: Authenticate

If you already have an account:

```bash
outlit auth login
```

New to Outlit? Start signup from the terminal:

```bash
outlit auth signup
```

<Info>
  On macOS/Linux, the CLI stores your API key at `~/.config/outlit/credentials.json` with `0600` permissions. You can also pass a key directly with `--key` or set the `OUTLIT_API_KEY` environment variable.
</Info>

## Step 3: Connect Your First Source

Most teams start with the browser SDK to capture website visits, form submissions, and engagement automatically. Choose your framework to get started:

<CardGroup cols={3}>
  <Card title="Script Tag" icon="code" href="/tracking/browser/script">
    Fastest setup — one snippet
  </Card>
  <Card title="React" icon="react" href="/tracking/browser/react">
    Hooks and context provider
  </Card>
  <Card title="Next.js" icon="n" href="/tracking/browser/nextjs">
    App Router and Pages Router
  </Card>
  <Card title="Vue" icon="vuejs" href="/tracking/browser/vue">
    Composition API plugin
  </Card>
  <Card title="Nuxt" icon="n" href="/tracking/browser/nuxt">
    Auto-imported composables
  </Card>
  <Card title="SvelteKit" icon="s" href="/tracking/browser/sveltekit">
    Stores and lifecycle hooks
  </Card>
  <Card title="Astro" icon="a" href="/tracking/browser/astro">
    Islands and SSR support
  </Card>
  <Card title="Angular" icon="angular" href="/tracking/browser/angular">
    Injectable service
  </Card>
  <Card title="Node.js" icon="node-js" href="/tracking/server/nodejs">
    Server-side tracking
  </Card>
</CardGroup>

Then connect external sources like Stripe, PostHog, Slack, Gmail, Fireflies.ai, Google Calendar, Supabase Auth, Clerk, and more from your [integrations dashboard](https://app.outlit.ai/integrations).

<Info>
  Outlit is infrastructure, not a CRM. It syncs your sources, resolves identities across them, structures the data into customer context, and gives you a query layer via CLI, MCP, and API.
</Info>

<Warning>
  Outlit uses cookies to track visitors across pages. If you need GDPR compliance, show a consent banner before loading the tracker. [See consent management →](/tracking/browser/script#consent-management)
</Warning>

## Step 4: Query Your Data

Once the tracker is live and you have some traffic, use the CLI to explore:

```bash
# List your customers
outlit customers list

# Get a full customer profile
outlit customers get acme.com --include users,revenue,recentTimeline

# Search with natural language
outlit search "who signed up this week"

# Run SQL directly
outlit sql "SELECT domain, billing_status, mrr_cents FROM customer_dimensions ORDER BY mrr_cents DESC LIMIT 10"
```

<Tip>
  **Set up coding agents** to query customer context directly from Claude Code, Codex, or Gemini CLI:

  ```bash
  outlit setup --yes
  ```

  This auto-detects installed coding agents and installs the `outlit` skill. For MCP clients, use the direct [MCP Integration →](/ai-integrations/mcp) guide.
</Tip>

## What happens automatically?

Once the browser SDK is installed, Outlit captures without any additional code:

| What | How |
|------|-----|
| **Pageviews** | Every page navigation, with URL, title, and referrer |
| **Form submissions** | Field values with automatic sensitive-field sanitization |
| **Identity** | Auto-identifies visitors when forms contain an email field |
| **Engagement time** | Active time on each page (not just tab-open time) |
| **Attribution** | UTM parameters, referrer, and landing page on first visit |
| **Company enrichment** | IP-based company identification for B2B visitor intelligence |

Once you connect additional sources, Outlit merges those product, billing, support, and conversation signals into the same customer context.

## Next Steps

<CardGroup cols={2}>
  <Card title="OpenClaw + Outlit" icon="lobster" href="/use-cases/openclaw">
    Automated customer intelligence with cron jobs and heartbeat monitoring
  </Card>
  <Card title="How It Works" icon="circle-info" href="/tracking/how-it-works">
    Understand how Outlit collects, resolves, and connects customer data
  </Card>
  <Card title="Customer Context Graph" icon="circle-nodes" href="/concepts/customer-context-graph">
    Learn about the unified data model behind Outlit
  </Card>
  <Card title="CLI Commands" icon="terminal" href="/cli/commands">
    Full reference for querying customer data
  </Card>
</CardGroup>
