---
title: "AI Agent Setup"
description: "Install Outlit skills for Claude Code, Codex, Gemini CLI, Droid, OpenCode, Pi, and other coding agents"
---

The `outlit setup` command installs the `outlit` skill for coding agents. It does not configure MCP clients.

If you want Cursor, VS Code, Claude, or another MCP client, use the direct [MCP Integration](/ai-integrations/mcp) guide instead.

## Supported Agents

| Agent | Command | What it does |
|-------|---------|--------------|
| Claude Code | `outlit setup claude-code` | Installs `outlit` for the `claude-code` skills target |
| Codex | `outlit setup codex` | Installs `outlit` for the `codex` skills target |
| Gemini CLI | `outlit setup gemini` | Installs `outlit` for the `gemini-cli` skills target |
| Droid | `outlit setup droid` | Installs `outlit` for the `droid` skills target |
| OpenCode | `outlit setup opencode` | Installs `outlit` for the `opencode` skills target |
| Pi | `outlit setup pi` | Installs `outlit` for the `pi` skills target |
| OpenClaw | `outlit setup skills` | Use the interactive installer and choose OpenClaw |
| Manual / multi-agent | `outlit setup skills` | Launches the interactive Outlit skills installer |

## Auto-Setup

Detect installed coding agents and install `outlit` for all of them:

```bash
outlit setup --yes
```

Without `--yes`, the CLI lists detected agents before installing.

### JSON Output

```json
{
  "detected": ["claude-code", "opencode"],
  "configured": ["claude-code", "opencode"],
  "failed": [],
  "runner": "npx"
}
```

## Individual Setup

### Claude Code

```bash
outlit setup claude-code
```

### Codex

```bash
outlit setup codex
```

### Gemini CLI

```bash
outlit setup gemini
```

### Droid

```bash
outlit setup droid
```

### OpenCode

```bash
outlit setup opencode
```

### Pi

```bash
outlit setup pi
```

### Interactive Skills Installer

Use this when you want to pick agents manually or optionally add `outlit-sdk`:

```bash
outlit setup skills
```

This opens the interactive `skills add` flow scoped to `github.com/OutlitAI/outlit-agent-skills`.

## Diagnostics

The `doctor` command checks your CLI auth plus coding-agent skill installs:

```bash
outlit doctor
```

### Checks Performed

| Check | What it verifies |
|-------|-----------------|
| CLI version | Whether you're running the latest version |
| API key | Presence and format of your API key |
| API validation | Live test call to verify the key works |
| Integrations | Whether Outlit can read integration status |
| Per-agent checks | Whether detected coding agents already have the `outlit` skill |

### Example Output

```
  Outlit Doctor

  ✓ CLI version: v1.4.1 (latest)
  ✓ API key: Found (ok_ab...1234) via config
  ✓ API validation: Key is valid
  ! Claude Code: Installed, but Outlit skill not found
  ✓ Codex: Outlit skill installed

  Everything works, 1 suggestion(s).
```

### JSON Output

```bash
outlit doctor --json
```

```json
{
  "ok": true,
  "checks": [
    { "name": "CLI version", "status": "pass", "message": "v1.4.1 (latest)" },
    { "name": "API key", "status": "pass", "message": "Found (ok_ab...1234) via config" },
    { "name": "API validation", "status": "pass", "message": "Key is valid" },
    { "name": "Claude Code", "status": "warn", "message": "Installed, but Outlit skill not found" }
  ]
}
```

Filter for failures:

```bash
outlit doctor --json | jq '.checks[] | select(.status == "fail")'
```

### Exit Code

`doctor` exits with code `1` if any check fails, which makes it useful in CI:

```bash
outlit doctor || echo "Setup issues detected"
```

## Shell Completions

Generate completion scripts for your shell:

<Tabs>
  <Tab title="Bash">
    ```bash
    outlit completions bash >> ~/.bash_completion
    source ~/.bash_completion
    ```
  </Tab>
  <Tab title="Zsh">
    ```bash
    outlit completions zsh >> ~/.zshrc
    exec zsh
    ```
  </Tab>
  <Tab title="Fish">
    ```bash
    outlit completions fish > ~/.config/fish/completions/outlit.fish
    ```
  </Tab>
</Tabs>
