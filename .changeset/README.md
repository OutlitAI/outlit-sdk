# Changesets

This directory contains changesets for the monorepo. Changesets are used to manage versioning and changelogs.

## Creating a Changeset

To create a changeset, run:

```bash
npm run changeset
```

Follow the prompts to describe your changes.
