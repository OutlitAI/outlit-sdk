---
"@outlit/tools": patch
"@outlit/cli": patch
---

Add `outlit customers credits <customer>` and sync its generated contract with Core. The command returns imported customer credit pools, observation state, reset timing, and deterministic burn summaries. Stable publication requires the matching Core change to be deployed and verified first.
