---
"@outlit/cli": patch
---

Add --doc-types, --source-types, --source-type, and --source-id flags to `outlit search`. Query is now optional when --source-type and --source-id are provided for direct source lookup.
